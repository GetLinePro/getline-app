<?xml version="1.0" encoding="utf-8"?>
<!--
  GL-12 / #39: LogcatService is debug-only. Declaring it here (not in main)
  keeps specialUse and FOREGROUND_SERVICE_SPECIAL_USE out of release by
  construction. Do not move either back to main.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <service
            android:name=".LogcatService"
            android:exported="false"
            android:label="@string/clash_logcat"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="explanation_for_special_use" />
        </service>
    </application>
</manifest>
